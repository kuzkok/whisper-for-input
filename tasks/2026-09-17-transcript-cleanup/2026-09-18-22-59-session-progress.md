# Сессия 2026-09-18 — разбор выхода 17.09 с пользователем, сравнение haiku/sonnet, переключение слота haiku на GLM-5.3-flash

Статус: в работе

Связанные документы:
- [2026-09-17-17-21-analysis.md](./2026-09-17-17-21-analysis.md) — исходный анализ
- [2026-09-18-15-09-session-progress.md](./2026-09-18-15-09-session-progress.md) — предыдущая сессия

## Где остановились в прошлой сессии

Скилл реализован целиком (пункты 1–5 анализа), обкатан на двух реальных встречах, оба прогона rc 0. План был: взгляд пользователя на выходы (критерий приёмки 8), потом снятие чернового статуса анализа и коммит по команде пользователя.

## Что сделано в этой сессии

### Находка: латинизированный сленг и смешение скриптов
- Пользователь указал реплику, где ASR выдал русский сленг латиницей: чистка оставила его латиницей и вдобавок породила слово в смешанном скрипте (кириллическая буква внутри латинского слова). Пара для этого слова в глоссарии была — модель не связала её через границу скрипта.
- Правки: в контекст-файл пользователя добавлены латинские формы распознавания в существующую пару; в `chunk-prompt.md` — класс «латинизированный кириллический сленг» (склоняемая латиница = ASR-сленг, сопоставлять с парами фонетически) и запрет смешанных скриптов (self-check п.2 дополнен).

### Разбор 12 маркеров пользователя в .clean.txt (17.09)
- Пользователь разметил проблемные места форматом `###как есть|как правильно###`; сверены с исходником (что дал ASR) и контекст-файлом (была ли опора).
- Три класса: (А) дыры политики — пары не применяются к словоформам (множественное число распознанного не совпало с парой в единственном), искажённые имена не восстанавливаются по таблицам (обращение, фамилия кириллицей, фамилия латиницей), достраивание отсутствующего отчества (ASR склеил обращение к двум участникам в одну строку «имя + фамилия-паттерн», чистка достроила отчество); (Б) правки без опоры с выдуманными промежуточными формами (три случая); (В) ожидаемое «оставить» без пар в глоссарии (включая одну спорную пару, подтверждённую пользователем по аудио и переведённую в уверенные).
- Подтверждено пользователем: склейка из п. (А) — в речи было обращение к двум участникам по именам, ASR слил их в одну «фамилию с отчеством».

### Сравнительный прогон фазы 2 на sonnet (обёртка /tmp/phase2_sonnet.py, вне репо)
- Обёртка импортирует cleanup_transcript как модуль, подменяет PHASE2_MODEL, переиспользует карту из существующего .clean.txt (фаза 1 не выполнялась). Выход: `…17.09.26.sonnet.txt` у пользователя.
- Итог по 14 позициям (12 маркеров, один ×2, плюс перепутанная аббревиатура ×2 и латинизированный сленг): sonnet закрыл 9/14, haiku 0/14. Sonnet сверх того сам исправил ~12 непомеченных омофонов и искажённых имён.
- Цена sonnet: одна новая реальная ошибка ([00:11:40] «пройди то»→«про то», потеря слова), полный прогон ~20 минут, один чанк таймаутил на 180 с и второй раз на 420 с ушёл в ретрай, дороже по счётчику роутера.
- Оговорка честности: у sonnet были два свежих правила (латиница-склонение, смешение скриптов), которых у haiku в его прогоне не было; на 12 маркеров они не влияли, кроме №8/№11.

### Правки по итогам разбора (согласованы с пользователем)
- `transcript-cleanup/chunk-prompt.md`: пары глоссария покрывают словоформы (падежи/числа); искажённые ASR имена восстанавливаются фонетически по таблицам и парам, обращение — до формы обращения, не до полного имени; запрет промежуточных форм («вертуалки», «австраструктура») — замена только на слово из пары/канона/общеизвестную форму; запрет достраивать имена и сливать два имени в одно (пример на именах синтетической фикстуры внесён в промпт).
- Контекст-файл пользователя: в раздел ошибочных форм добавлена перепутанная аббревиатура; 9 новых пар по классам находок — искажённые формы фамилии (4 варианта, включая латиницу), сленговые словоформы, омофоны, бренд, внутренний жаргон, жаргон латиницей. Перечень пар — в контекст-файле пользователя, вне репо.
- `transcript-cleanup/cleanup_transcript.py`: PHASE2_TIMEOUT_S 180→420, PHASE2_BUDGET_USD 0.30→1.50, оба с комментарием о происхождении чисел. PHASE2_MODEL остался "haiku" — пользователь переключил подложку слота haiku на GLM-5.3-flash на своей стороне (решение: остаёмся на слоте haiku, sonnet не переводим).

### Контрольный прогон основным скриптом после переключения слота
- Полный боевой путь: `cleanup_transcript.py <src> <ctx> -o …flash.txt --yes` (пользователь явно попросил именно основной скрипт, не обёртку). rc 0.
- Карта спикеров воспроизвела подтверждённую в прошлой сессии карту один в один (4 из 5, SPEAKER_00 — null). Ретраев не было ни в одной фазе.
- По всем 14 позициям — 14/14: восстановлены обращения и фамилии (включая латиницу → кириллицу), применены все новые пары (включая словоформы), склейка двух имён разодрана по новому правилу, перепутанная аббревиатура исправлена, латинизированный сленг приведён к паре. На паре «жаргон → канонический термин» точнее sonnet, который взял смежный термин из глоссария вместо нужного. Сонетовскую порчу «пройди» не воспроизвёл.
- Непомеченные омофоны тоже берёт (повелит→выделит, крятки→грядки, вятка→ветка, нервники→нейронки, тело→дело, Cuber→Kubernetes, Ложе→Лучше и др.): 82 реплики из 145 с лексическими заменами против 74 у sonnet.
- Сомнительные правки flash, ожидают проверки пользователем (список для следующей сессии): [00:25:23] «искусственного»→«космического» и «то»→«тому»; [00:26:30] «X-моделях»→«этих моделях» (в речи могло быть «на ONNX-моделях»); [00:33:36] «дошенного»→«двоенного» (возможно «двойного»); [00:36:40] «подходящий»→«подходящих»; [00:21:37] «видео»→«виде»; [00:15:52] «разуме»→«разом»; [00:06:21] «то»→«того».

## Что подтверждено

- 35 тестов (unit + CLI с фейковым claude -p) — OK после всех правок промпта и констант.
- Sonnet-прогон фазы 2: rc 0, верификация чистая; единственная новая ошибка — потеря слова в [00:11:40].
- Flash-прогон полным скриптом: rc 0, верификация чистая, карта идентична эталонной, ретраев не было; 14/14 проблемных позиций закрыто (сверка word-diff'ом по исходнику).
- Подтверждённая пользователем интерпретация [00:30:40]: в речи было обращение к двум участникам по именам, ASR слил их в «фамилию с отчеством» — чистка не должна разворачивать склейку обратно в одно имя.

## Текущее состояние кода

- `transcript-cleanup/cleanup_transcript.py` — константы фазы 2: 420 с / $1.50 (с комментариями); модель фазы 2 — "haiku" (слот мапится на GLM-5.3-flash у пользователя). Остальное без изменений с прошлой сессии.
- `transcript-cleanup/chunk-prompt.md` — политикой добавлены: словоформы пар, восстановление имён фонетически, запрет промежуточных форм, запрет достраивания/сливания имён, латинизированный сленг, запрет смешанных скриптов; self-check п.2 расширен.
- `transcript-cleanup/map-prompt.md`, `transcript-cleanup/SKILL.md` — SKILL.md дополнен сводкой про латинизированный сленг; map-prompt не менялся.
- `transcript-cleanup/tests/` — 35 тестов, зелёные (системный python3, ../.venv не нужен).
- `install.sh`, `CLAUDE.md` — изменены ещё в прошлой сессии (симлинк скилла, layout), в этой сессии не трогались.
- Git: `transcript-cleanup/` целиком untracked, `CLAUDE.md` и `install.sh` изменены; коммитов не было — ждёт команды пользователя.
- У пользователя рядом с исходниками: `…17.09.26.clean.txt` (с маркерами ###…### — правки не внесены), `…17.09.26.sonnet.txt`, `…17.09.26.flash.txt` — все приватные, вне репо.

## План на следующую сессию

1. Проверка сомнительных правок flash из контрольного прогона (список из 7 мест в «Что сделано») — по аудио/по смыслу с пользователем; находки уйдут в пары глоссария или обратно в промпт.
2. Решение пользователя о рабочем выходе 17.09: принять flash-выход как основной (маркеры ###…### в старом .clean.txt уже отработали своё) или править вручную.
3. Прогон 15.09 на новом слоте — та же сверка по известным местам (фаза 2 на 17.09 опознала всех, кого можно; на 15.09 был неопознан пятый спикер из-за склейки диаризацией — карта там строится фазой 1, слот sonnet не менялся, ожидания те же).
4. По итогам 1–3 — снятие чернового статуса анализа (строка «Статус:» и слово в H1 `2026-09-17-17-21-analysis.md`).
5. Коммит по явной команде пользователя (transcript-cleanup/ + CLAUDE.md + install.sh).
6. Отложено: контрольный прогон при появлении подписки Claude (поведение `--max-budget-usd` на подписке не проверено).

## Замечания для следующего агента

- Инварианты от пользователя (carry-forward): таймкоды в выходе сохранять; сторону встречи в метки не добавлять; таймаут фазы 1 (300 с) не поднимать; бюджетные константы — пороги фиктивного счётчика роутера, реальные деньги не списываются.
- Слоты роутера: haiku теперь мапится на GLM-5.3-flash (решение пользователя в этой сессии), sonnet не трогали. Выводы о «haiku хуже sonnet» относятся к старой подложке слота haiku — после переключения flash закрыл 14/14 и обогнал sonnet по точности (JSON-ки vs GeoJSON-ки, без порчи «пройди»).
- Обёртка эксперимента `/tmp/phase2_sonnet.py` — временный файл вне репо, паттерн (импорт модуля + подмена констант + карта из существующего выхода) пригодится для будущих A/B прогонов одной фазы.
- Фаза 1 на sonnet-слоте в этой сессии прошла с первого раза дважды; прежняя нестабильность роутера (сжигание $0.50 первой попыткой) не воспроизвелась, но ретраи в коде — не гейт.
- Конфиденциальность (carry-forward): реальные транскрипты, контекст-файл, эталон и выходы прогонов — приватны, вне репо; имена участников и названия компаний в артефакты репо не записывать (этот чекпоинт описывает сверки количественно). Фикстуры тестов — синтетика.
- `--json-schema` в `claude -p` передаётся строкой-аргументом, ответ разбирается как `structured_output` (carry-forward).
- Пользователь предпочитает для контрольных прогонов основной скрипт целиком, а не обёртки над фазами — экономия фазы 1 не стоит потери боевого пути.
